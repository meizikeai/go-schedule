app:
  name: go-schedule

lb:
  test: "127.0.0.1:3000"

mysql:
  default:
    master:
      - "database:password@tcp(127.0.0.1:3306)/tablename"
    slave:
      - "database:password@tcp(127.0.0.1:3306)/tablename"
    max_idle_conns: 100
    max_open_conns: 200
    conn_max_lifetime: 60

redis:
  default:
    addrs:
      - "127.0.0.1:6379"
    password: ""
    db: 0
    pool_size: 40
    min_idle_conns: 10

kafka:
  default:
    brokers:
      - "127.0.0.1:9092"
